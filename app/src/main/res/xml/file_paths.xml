<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External storage downloads directory -->
    <external-path
        name="external_downloads"
        path="Download/" />

    <!-- External storage root (for custom folder selections) -->
    <external-path
        name="external_storage"
        path="." />

    <!-- App's external files directory -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- App's external cache directory -->
    <external-cache-path
        name="external_cache"
        path="." />
</paths>